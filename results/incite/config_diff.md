# INCITE: config vs plan diffs

Mandatory ledger (docs/INCITE_PLAN.md lesson 4): every hyperparameter that
deviates from docs/INCITE_DESIGN.md / docs/INCITE_PLAN.md, or that those
documents leave unspecified, is recorded here THE DAY it happens. The CREST
stage-B lr ran at half spec until a human asked; that does not repeat.

| date | parameter | plan value | actual value | reason |
| --- | --- | --- | --- | --- |
| 2026-08-28 | relation.lambda | unspecified (design D: "L = L_entity + lambda*L_relation") | 1.0 (v1), 0.0 (phase1) | design fixes the mechanism, not the number; 1.0 = equal weighting, the simplest defensible default. Phase 1 runs entity-only by definition (PLAN phase 2.3 adds the lever). |
| 2026-08-28 | support.class_prior | unspecified (design B: "down-weight them with the class prior") | 0.1 | no prior estimate exists in-harness; 0.1 down-weights negatives to 0.9. Revisit when the usage probe (PLAN phase 2.2) runs. |
| 2026-08-28 | support.neg_per_pos | implied 4 (design 4: "at most 80 support rows" with K=16 -> 16*(1+4)=80) | 4 | back-derived from the 80-row budget; the design never states it directly. |
| 2026-08-28 | support.ball_cap | unspecified (design B estimates ~100 entities per 3-hop ball on WN18RR) | 1024 | dense graphs (Hetionet-like) explode the 3-hop ball; the cap bounds support building. Seeded subsample beyond the cap. |
| 2026-08-28 | support.build_batch_size | not a plan parameter | 16 | memory/time chunking only (crest/bank.py precedent: sampling is drawn chunk-size-invariantly). |
| 2026-08-28 | train.val_samples | unspecified | 500 per DEV10 graph per validation | TRIX's own fast_test=500 convention; full DEV10 eval at every val_interval would dominate wall clock. |
| 2026-08-28 | DEV10 selection scalar | PLAN: "one mean per suite group, never one number" | per-group means logged at every validation; checkpoints ORDERED by the unweighted mean of the three group means | selection needs a total order; the reported quantities remain the group means (incite/pretrain.py logs all three per checkpoint). |
| 2026-08-28 | DEV10 validation split | PLAN: "zero-shot DEV10" | each DEV10 graph's VALID split, never test | test-split selection would leak the benchmark into checkpoint choice. |
| 2026-08-28 | gradient flow through support rows | PLAN lesson 3 (specified deviation) | rows no_grad + detached, refresh_interval 500, cost gate 0.20 | recorded per the PLAN's own instruction: known deviation, first suspect if support underperforms. |
| 2026-08-28 | walk token / pooling details | design C: "small GRU", "pool the outputs into the states of the visited entities and relations" | per-step token = anon-relation emb + anon-entity emb; GRU outputs mean-pooled per visited entity / traversed relation, linear-projected, ADDED to initial states | the design fixes protocol and pooling targets, not the token layout; incite/walks.py documents the exact construction. |
| 2026-08-28 | relation-step direction | design A writes m(r_j) = sum over incoming | implemented in TRIX's EXECUTED direction (rspmm aggregates into edge_index[0]; ht/th are each other's transposes) | the layer gate compares against the pinned layer as it actually runs; see incite/layers.py docstring. Learned per-channel weights make the labeling difference immaterial. |
| 2026-08-28 | model.dim / TRIX recipe dims | TRIX ships 32 with per-block hidden lists | dim 32 everywhere, 6 alternating rounds (one entity layer + one relation step per round) | design A/4 fixes 6 rounds of the alternation; TRIX's own 3+2+4 block split does not map onto it. |
| 2026-08-28 | train.batch_size / train.accum_steps | recipe batch 32 | micro-batch 16, accum_steps 2 (effective 32) | measured OOM at batch 32 on the 15.6 GiB GPU (15.0 GiB allocated, first step). Micro-batches come from the same drawn graph and are loss-averaged before the optimizer step, so the objective per update matches the recipe up to negative-sample draws. |
| 2026-08-28 | (diagnosis correction) | — | — | the batch-8 commit blamed rspmm's saved sorted pair lists for the OOM. Re-reading the hot path: pair_sum folds batch into features, edge tensors are ~13 MB per call. The memory is ordinary retained (batch, V, d) activations across 6 rounds x ~9 module calls. Fix path: per-round activation checkpointing, then batch 32 in one piece. |
| 2026-08-28 | train.checkpoint_activations | not a plan parameter | flag added, default OFF; commented out in both configs ("enable after Phase 1") | per-round activation checkpointing (torch.utils.checkpoint, use_reentrant=False) wrapping each entity+relation round in incite/model.py::_round -- trades one trunk recompute in backward for the retained (batch, V, d) activations that OOM batch 16+. NOT active during the phase-1 pretrain: the live run executes a pre-optimization snapshot and its config never set the flag. Recompute is deterministic (no dropout in the trunk, walk features sampled once before round 1 from a fresh seeded Generator); CPU tests assert all grads allclose 1e-6 vs the uncheckpointed run, walks ON (incite/tests/test_checkpointing.py). |
| 2026-08-28 | FactorizedRelationStep fused forward | plan fixes the math (design A factorization), not the launch schedule | fused=True default: ~12 pair_sum launches per round -> 2 (count channel stacked onto its role channel's feature axis; head+tail incidence lists concatenated with node ids offset by V, relation ids by R) | forward-path rewrite only: parameters and state_dict keys UNCHANGED, so phase-1 checkpoints load strictly with no key mapping. Per-slot pair lists, order, diagonal corrections and update summation order are identical to the unfused path (kept as _forward_unfused); CPU fused-vs-unfused outputs and grads allclose 1e-6 (bitwise-equal on the fallback), layer gate (1e-5 vs materialized reference and pinned TRIX) unchanged (incite/tests/test_fusion.py). NOT active during the phase-1 pretrain (snapshot predates it); evals pick it up on their next run. |
| 2026-08-28 | train.batch_size / accum_steps / checkpoint_activations | recipe batch 32 | 32 / 1 / yes | the batch-8 x 4 run was abandoned at step 600 (also ended by the Xid-79 GPU bus drop). With per-round checkpointing and the fused relation step GPU-verified (38/38 including layer gates), the recipe batch runs in one piece. This supersedes the two micro-batch rows above; the phase-1 run of record uses this line. |
| 2026-08-28 | train.steps | TRIX pretrain: 800,000 (num_epoch 10 x batch_per_epoch 80,000, pretrain_entity.yaml) | 20,000 | MISSED DEVIATION, caught by the user asking how long TRIX trained. 20,000 was inherited from the CREST plan, where it sized a readout, not a from-scratch pretrain. Consequence: the phase-1 eval compares INCITE at 2.5% of TRIX's budget against TRIX's full-budget checkpoint -- a budget handicap, not a clean architecture comparison. The gate verdict must be read with this handicap stated. |
| 2026-08-28 | (correction to the row above) | TRIX budget is NOT 800,000 | paper: 10 epochs x 10,000 steps; code: islice caps an epoch at loader exhaustion, ~17,000 batches single-GPU; no early-stop logic, "early stopping" = best-epoch reload | true TRIX budget ~100k-170k optimizer steps (5-8x ours), likely at effective batch 64+ under multi-GPU. Caught by the user reading the paper against the config. Extending INCITE to the paper budget (~100k steps, ~2 GPU-days) is feasible; 800k never was. |
| 2026-08-28 | (paper read, arXiv 2502.19512) | — | — | paper confirms: 10 x 10,000 steps, batch 32, AdamW 5e-4, single A5000/A6000/4090-class GPUs. The "effective batch 64+ multi-GPU" speculation in the row above is withdrawn. Handicap is exactly 5x steps at matched batch and comparable hardware. The paper also claims TRIX is ~10x slower than ULTRA at entity prediction; this harness measures 1.9x suite wall clock -- a criterion-B cost discrepancy for the report. |
| 2026-08-29 | phase 2.1 warm start | PLAN says "add in this order", start point unspecified | weights-only init from phase-1 incite_best.pth (step 17k); walk module fresh; fresh optimizer, step 1 | --init_from in incite/pretrain.py, INCITE_INIT_FROM knob. The trunk is the validated floor; retraining it from scratch with walks would confound the lever with a new trunk draw. Deviation from a fresh-start reading is deliberate and recorded. |
| 2026-08-29 | crash-resume semantics | intended: continue at checkpoint step | was: weights-only, counter restarted at 1 (first exercised on the walks-run crash; would have trained 29k total). Fixed: counter continues; optimizer and RNG restart fresh, so a resumed run's draw sequence differs from an uninterrupted one. The walks run resumes from the step-8000 best (the mislabeled resume overwrote the step-9000 last); net loss 1,000 steps. |
| 2026-08-29 | synth block (phase 2.1b) | not a plan parameter: PHASE21_RESULT.md names "symmetry-breaking supervision in the mix (synthetic automorphic graphs with labeled answers)" as future work, with no numbers | `synth: {enabled: yes, fraction: 0.05, instances_per_step: 16, seed: 2048}` in configs/incite_phase21b_walksynth.yaml; module defaults also carry pool_size 32 and palette 8 | fraction 0.05 = the "small fraction" of PHASE21_RESULT.md: the lever under test is still the walks lever against the phase-1 floor, so the mix must stay recognizably the pretraining mix (935 of 20,000 steps are synthetic at this setting). instances_per_step 16 = one union batch small enough to be free next to a 32x512 real step (0.2s/step on CPU, 471 nodes / 1024 edges at dim 32). seed 2048 is deliberately NOT the eval instances' 1024 (diagnostics/generate_petals.py), so the exact instances differ from the diagnostic's. pool_size 32 gives the per-step union something to sample from without replacement; palette 8 bounds the colour ids. |
| 2026-08-29 | synth instance family overlaps the PETALS diagnostic | — | BY DESIGN: same generator family (randomized colourings/cycle sizes/tail lengths, different seed) | stated up front so no one reads it as generalization. The experiment asks only whether symmetry-breaking supervision can align the walk module's tie-breaking with truth AT ALL; a post-training PETALS number is a TRAIN-DISTRIBUTION score and must be reported as such. The kill switch's second half (DEV10 within noise of the floor) and the 41-graph benchmark are what carry any claim. |
| 2026-08-29 | synthetic steps and gradient accumulation | recipe: batch_size x accum_steps micro-batches per optimizer step | synthetic steps IGNORE accum: one union batch of instances_per_step instances = one optimizer step | the union already batches k instances into a single forward; accumulating several unions would re-average the same distribution at k times the cost. Documented in incite/synth.py::synth_step_loss and in the pretrain loop. |
| 2026-08-29 | union-batch relation states | — | k instances share ONE relation vocabulary and one forward, so the factorized relation step sums node features over every incidence pair in the union: instances contribute a bulk term to each other's relation states | deliberate batching approximation (the alternative is k separate forwards per synthetic step). Entity message passing cannot cross components, and both candidates of a query sit in the same component and see the same relation states, so the within-instance true/false asymmetry -- the only thing the loss scores -- is untouched. |
| 2026-08-29 | synthetic step scheduling / RNG | — | the synthetic/real coin is a pure function of (synth.seed, step) from a fresh generator; instances come from a generator seeded synth.seed + step | keeps resume classifying and rebuilding each step number identically, and leaves pick_gen/pos_gen unconsumed by the coin. Consequence, recorded: a synthetic step draws no real graph, so pick_gen advances once less than it would in a synth-off run -- the two runs' draw sequences differ by construction. |
| 2026-08-30 | NELL995 source (S8 4-graph mix) | TRIX loader URL (RED-GNN repo, transductive/data/nell) | same repo, tree moved under Static/; raw files pre-seeded into data/roots/incite/pretrain/nell995/raw so the pinned loader's download() never runs | upstream drift (404), the SEMMA-Wikidata failure class. Pinned repo untouched. First S8 attempt also fixed a KeyError: prepare_pretrain_root now skips names without a pre-seeded raw mapping. |
| 2026-08-30 | 4-graph run batch (incite_phase1_4g.yaml) | recipe batch 32 single piece | micro-batch 16 x accum 2 (effective 32) | NELL995's 74.5k nodes OOM the single-piece batch even with checkpointing (2.84 GiB recompute alloc, S8 log). Memory-forced, same class as the early phase-1 rows. Applies to the 4g run only. |
| 2026-08-31 | synth.prior "rules" parameter ranges (RULES_RANGES, incite/synth.py) | proposal fixes families and rough ranges (entities log-uniform ~100-2000, relations 8-64, types 4-16, confidences ~0.6-0.95, incompleteness 10-40%, chain cap 3, union of 16 < ~50k edges), leaves the rest open | entities log-U 100-2000 / relations U 8-64 / types U 4-16 (sigs 1-3 types a side) / entity-Zipf 0.7-1.1, relation-Zipf 0.5-1.0 / density 0.8-1.6 facts per entity, base cap 900, instance cap 1400 facts, per-rule 4000 candidates/iter / rule counts hier 1-4, inv 1-3, sym 1-3, comp 1-4 (chain 2 or 3) / confidence U 0.6-0.95, drop U 0.1-0.4 / rule BODIES drawn by base-fact relation frequency | proposed ranges kept verbatim where given. Caps: 16 x 1400 x 2 (inverses) = 44.8k is the hard worst case under the 50k budget; sorted-prefix truncation keeps closures seed-pure, and the bounded chainer IS the prior's derivability semantics (tests re-run it). Frequency-biased bodies: unweighted bodies land on fact-poor relations and leave ~3% of facts derived (measured), weighted ~15-35% -- a rule prior needs its rules where the facts are. Rules-family steps also generate exactly instances_per_step instances (pool_size oversampling would double generation cost for the same distribution; petals keeps pool_size so its stream stays byte-identical). Full note: RULES_PRIOR.md. |
| 2026-09-01 | training seed of the v1 seed repeats (R3 "seed 1337", R4 "seed 7") | seed 1337 / 7 | 1024 | scripts/train_incite.sh hard-coded `--seed 1024`; INCITE_SEED was documented and forwarded but never read. The R3 run (output/incite-pretrain-v1-seed1024-dup-killed) drew the same graph sequence as R1 step for step and was killed at step ~2700. Fixed: the script now passes `--seed "${INCITE_SEED:-1024}"` and PROVENANCE.json records the value used. R3/R4 are cancelled; seed repeats move to the winner of the 2026-09-01 plan. |
| 2026-09-01 | checkpoint contents | model weights, step, dev10, seed | plus `optimizer` (AdamW state) and `lr` | a crash-resume now continues the AdamW moments; earlier resumes restarted them fresh (the ledger row of 2026-08-29 stands for those runs). |
| 2026-09-01 | lr schedule | recipe: constant 5e-4 | `--lr_schedule constant|linear|cosine`, `--lr_final`, `--warmup_steps`, `--schedule_start`; default constant (every run so far) | the L1/L2 continuations resume the 4-graph and floor runs from step 20,000 to 30,000 with a linear decay to 0 after a 500-step warmup (the resumed checkpoints carry no optimizer state), anchored at step 20,001 so a crash-resume continues the same decay. Both DEV10 curves still rose at 20k under the constant rate. |
| 2026-09-01 | checkpoint retention | best + last | `--keep_every N` also keeps incite_step<N>.pth | checkpoint averaging over the decay phase. |
| 2026-09-01 | test-time re-ranking (incite/rerank.py) | not a plan parameter | `--rerank_k`, `--rerank_weight` on incite/run.py; k and weight chosen on the DEV10 VALID splits (diagnostics/rerank_dev.py), stop rule: must lift the selection scalar by >= 0.002 over k=0 | reverse-direction logit of the top-k eligible candidates added to the forward logit, non-members imputed with the block minimum; the shared rank definition is untouched (docstring proves a target outside the block keeps its rank). Any table built from re-ranked dumps states k, weight and the ~(k+1)x eval cost. |
| 2026-09-01 | score ensembles (`--ckpt a,b,c`) | not a plan parameter | mean of member logits, support-off, members built from one config, lever tensors (readout.*, walk_module.*) ignored | an ENSEMBLE row on any table, labelled so. |
| 2026-09-01 | checkpoint selection, standing concern | PLAN: zero-shot DEV10 valid splits | unchanged for now; E1/L1/L2 also evaluate the LAST checkpoint | DEV10 graphs are ten of the 41 benchmark graphs; TRIX selects on its pretraining mix. The best-vs-last gap of the selection scalar is small (floor 0.4156 vs 0.4110, 4g 0.4244 vs 0.4233) but a reviewer will object. After the decay continuations the last checkpoint becomes the headline candidate and DEV10 is diagnostic only. |
| 2026-09-01 | half-link masking (`--mask_answer_p`, `--mask_query_p`; incite/model.py::mask_halflinks) | not a plan parameter; motivated by Gregucci et al. 2026 (arXiv 2606.18001) and results/incite/halflink.json | M1: 0.3 / 0.3 on a 10k-step continuation of the 4-graph run, paired against L1 (decay only, same start) | per-row coins are a pure function of (seed, step, micro). Rationale: 28 percent of test queries in both groups have an answer without an edge of the query relation (SQUA) and score 0.17-0.29 MRR against 0.58-0.61 with a seen answer half; pretraining positives almost always carry a seen answer half. 0.3/0.3 puts the training scenario mix near the test mix (about 35 percent unseen answers, 9 percent both unseen). The relation-level incidence pairs stay full-graph, as in TRIX. |
| 2026-09-01 | unary channel (`model.unary`; incite/model.py) | design A scores candidates from the head-conditioned trunk only | G1: `unary: yes`, warm start from the 4-graph last checkpoint, unary head fresh, 10k steps with linear decay | motivated by results/incite/reachability.json: 17 percent of ind_e answers (6 percent ind_er) lie beyond six hops of the query entity and score MRR below 0.01; the unlabeled pass (design B step 1) gives every entity a query-independent state that a second head scores with the query relation state. One extra unlabeled pass per graph at evaluation, cached. |
| 2026-09-01 | diagnostics | — | diagnostics/halflink.py (scenario shares and per-scenario MRR; shares reproduce the paper's Table 1 to three decimals), diagnostics/reachability.py (hop distance from query entity to answer, joined with rank dumps) | both CPU, container-only; results in results/incite/halflink.json and reachability.json. |
| 2026-09-02 | half-link masking dose 2 (M2) | M1: p_answer 0.3, p_query 0.3, no cap | p_answer 0.5, p_query 0.0, `--mask_answer_maxdeg 10` | M1 was net-negative and inverted (MASKING_RESULT.md): masked hubs (one target lost 484 edges) taught a popularity prior. Cap 10 keeps synthetic unseen-answer rows realistic; eligible shares among training positives: FB15k237 44%, WN18RR 79%, CoDExMedium 12%, NELL995 51% (output/check_eligible.py), giving about 27% unseen answers in the edge-weighted mix against 10-26% natural. Query masking dropped: unseen-query scenarios are the strong ones. |
